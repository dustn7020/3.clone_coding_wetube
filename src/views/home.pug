extends base.pug
include mixins/video


block content 
    div.video__grid
            each vi in videos 
                +video(vi)
            else
                span.empty__message No videos found